# Legacy sync tools

These scripts were used for one-off repository cloning, synchronization, repair, or debugging before the current GitHub App and repository API workflows were consolidated.

Keep them only as historical references. Production synchronization should use the application service and its authenticated API.
